# Supabase project-linking config for the production environment.
# See dev.env.example for the field explanation. Template, not a working
# config -- no project has been provisioned. Gated by deployment-plan.md
# §16's pre-ga.yml before this environment is ever deployed to.

SUPABASE_PROJECT_REF=
SUPABASE_URL=https://<project-ref>.supabase.co
