# Supabase project-linking config for the staging environment.
# See dev.env.example for the field explanation. Template, not a working
# config -- no project has been provisioned.

SUPABASE_PROJECT_REF=
SUPABASE_URL=https://<project-ref>.supabase.co
