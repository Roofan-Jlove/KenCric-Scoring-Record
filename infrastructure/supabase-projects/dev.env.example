# Supabase project-linking config for the dev environment.
# No secrets here - only the project reference (a public identifier),
# per repository-structure.md §11: "Project-linking config per
# environment -- no keys, only project refs."
#
# No real Supabase project has been provisioned for this environment yet
# (release-readiness-assessment.md: NO-GO, zero implementation deployed).
# This is a template, not a working config.

SUPABASE_PROJECT_REF=
SUPABASE_URL=https://<project-ref>.supabase.co
