# Supabase project-linking config for the pilot environment (Version 1
# closed pilot, deployment-plan.md §1). Deliberately a SEPARATE Supabase
# project from dev/staging/production -- same data-isolation principle
# as every other environment split in this corpus. Template, not a
# working config -- no project has been provisioned.

SUPABASE_PROJECT_REF=
SUPABASE_URL=https://<project-ref>.supabase.co
