{
  "name": "@kencric/web",
  "version": "0.0.0",
  "private": true,
  "description": "Web PWA (technology-stack.md ADR-T02: React + TypeScript + Vite, SPA/PWA, no SSR). Consumes backend/'s API and the shared/ Kotlin Multiplatform core's compiled JS bundle -- the latter does not exist yet anywhere in this repo (no Kotlin/Gradle toolchain has ever been available in this environment), so screens compose against a clearly-flagged stub interface (src/core/sharedCoreStub.ts) standing in for it until that bundle can actually be produced.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.5.0",
    "@testing-library/react": "^16.0.1",
    "@testing-library/user-event": "^14.5.2",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.2",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.0",
    "vite": "^7.0.0",
    "vitest": "^5.0.1"
  }
}
